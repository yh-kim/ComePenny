<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enterpaper.comepenny"
    android:versionCode="2"
    android:versionName="1.0.1" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.intent.action.MEDIA_MOUNTED" />


    <application
        android:name=".util.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/homo_appicon"
        android:label="호모띵쿠스"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".activities.MyInfoActivity" />
        <activity android:name=".activities.SettingActivity" />
        <activity android:name=".activities.WriteBoothActivity" />
        <activity
            android:name=".activities.WriteActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.LoadingActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tab.t2booth.BoothDetailActivity" />
        <activity android:name=".tab.t1idea.IdeaDetailActivity" />

        <fragement android:name=".tab.t1idea.IdeaFragment" />

        <activity
            android:name=".activities.LoginActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activities.SignUpActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activities.AdjustWriteActivity" />

        <!-- 위젯 리시버
        <receiver android:name=".util.AppWidgetMain" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/app_widget_main_info" />
        </receiver>
         -->
    </application>

</manifest>
